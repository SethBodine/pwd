{
  "name": "passgen",
  "version": "1.0.0",
  "description": "Cryptographically secure password generator — word, character, and sentence passphrase modes",
  "private": true,
  "scripts": {
    "dev":   "wrangler pages dev public --compatibility-date=2024-01-01",
    "deploy": "wrangler pages deploy public --project-name=passgen"
  },
  "keywords": ["password", "generator", "security", "passphrase", "entropy", "cloudflare-pages"],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
